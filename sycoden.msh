#### TODO {{{
# Think of something to improve.
# }}}

#### Mode Trap {{{
if [[ $MODE != "SCD" ]]; then
	return
fi
# }}}

#### SCD Development {{{

# SCD Navigation {{{

export SPJ_ROOT="$WORK/sycoden"
export SPJ=ListSum
if [[ $MODE == "SCD" ]]; then
  export PJ_ROOT=$SPJ_ROOT
  export PJ=$SPJ
fi

alias scd="mode <<< 3"

ss() {
	sv
	wt ss
	nodemon server.js
	wt
}

# }}}

#}}}
